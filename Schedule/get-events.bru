meta {
  name: get-events
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/schedule/events
  body: none
  auth: inherit
}

headers {
  X-Cybozu-Authorization: {{admin}}
  Content-Type: application/json
  X-Cybozu-Authorization2: 
}

assert {
  ~res.status: eq 200
  ~res.body: isJson
  ~res.body.title: startsWith abc
}

settings {
  encodeUrl: true
  timeout: 0
}
